# vendor

`three.min.js` — Three.js r160 (npm `three@0.160.1`, `build/three.min.js`), MIT licensed,
vendored so the atlas runs from `file://` and from any static host with no network
access and no build step. The upstream licence is kept alongside it as
`LICENSE-three.txt`.
